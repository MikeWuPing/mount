# 构建产物与运行证据（构建时确定性再生，不入库）
snapshot/
run_logs/
qemu_disk/
MountPkg/Application/Mount/Version.h
__pycache__/

# 测试资产（test_images/ 脚本生成，产物不入库）
test_images/*.vhd
test_images/*.vhdx
# 测试镜像生成中间产物（失败时可能残留）
test_images/*.iso
test_images/iso_stage/
test_images/dp_script.txt*
test_images/*.img

# 临时文件
*.ppm
*.log

# QEMU smoke test transient assets
qemu_disk.img
qemu_disk_smoke/

# 需求文档仅本地（不出现在公仓）
req.md

# 发布打包产物（本地生成，仅上传到 GitHub Release，不入库）
dist/

# 知乎发布材料（文章/题图/转换脚本，不入库）
docs/zhihu/
